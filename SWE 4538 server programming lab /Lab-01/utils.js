const generateRandom = () => {
    console.log('Generating random number generator: ' + Math.random());
}

const generateRandom2 = () => {
    console.log('Generating random number generator: ' + Math.random());
}

module.exports = {generateRandom, generateRandom2};